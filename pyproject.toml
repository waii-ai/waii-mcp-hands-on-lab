[project]
name = "waii-mcp-hands-on-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.7.1",
]

[tool.uv.workspace]
members = ["waii-mcp-hands-on-lab"]
